# One-off tooling to export the YOLOv8 detector to ONNX.
#
# Install this ONLY when producing weights/yolov8n.onnx — on a laptop, or once
# on the Pi and then uninstalled. It is not part of the runtime image.
#
#   pip install -r requirements.export.txt
#   python scripts/export_yolo_onnx.py --imgsz 480
#
ultralytics==8.3.0
torch==2.4.1
onnx==1.16.1
onnxslim==0.1.34
onnxruntime==1.19.2
