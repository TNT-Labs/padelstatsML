# One-off tooling to export the models to ONNX: the YOLOv8 detector and the
# OSNet re-identification network.
#
# Install this ONLY when producing the files in weights/ — on a laptop, or
# once on the Pi and then uninstalled. It is not part of the runtime image.
#
#   pip install -r requirements.export.txt
#   python scripts/export_yolo_onnx.py --imgsz 480
#   python scripts/export_reid_onnx.py
#
ultralytics==8.3.0
torch==2.4.1
onnx==1.16.1
onnxslim==0.1.34
onnxruntime==1.19.2
# Re-identification: only torchreid's osnet.py is used, and it needs just torch.
torchreid==0.2.5
gdown==5.2.0
